# elk

Observability config: `filebeat.yml` (Docker autodiscover, JSON log decoding) and
Kibana saved objects (data views, saved searches, Ops Overview + Pricing Analytics
dashboards) exported as ndjson. Built in Phase 10.
