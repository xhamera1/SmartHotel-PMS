# seed

Data seeding:

- demo seed — the defense-day narrative dataset (event weekend in Kraków lifting
  prices on specific dates), Phase 14.
- perf seed — bulk historical data (~100k reservations) loaded via `COPY` for
  load testing, Phase 11.
