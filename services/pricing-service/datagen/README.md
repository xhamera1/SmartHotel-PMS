# datagen

Synthetic dataset generator (CLI): config-driven (YAML + seed) simulation of demand,
bookings, events, and the revenue-optimal price target. Outputs parquet + metadata
sidecar to `artifacts/datasets/` (git-ignored); validated with pandera. Built in Phase 4.
