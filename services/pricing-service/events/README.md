# events

Event intelligence: `EventProvider` port with Ticketmaster and static-file adapters,
Gemini scoring client (structured output, versioned prompts under `prompts/`,
injection defenses, heuristic fallback scorer), per-night demand-indicator aggregation.
Built in Phase 7.
