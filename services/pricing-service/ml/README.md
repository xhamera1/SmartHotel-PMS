# ml

Machine-learning pipeline: shared feature module (single source of truth for
datagen/training/serving, exports `FEATURE_SCHEMA` + hash), Random Forest training CLI,
temporal-split evaluation with baselines and ablation, model registry, metric
regression gate for CI. Built in Phase 5.
